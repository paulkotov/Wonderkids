1) подключаем начальные настройки
		а) конфиги
		б) автозагрузка классов
2) обрабатываем route
		а) обрабатываем строку
		б) находим нужный контроллер и загружаем его
		в) передаем ему управление
3) контроллер
		а) подключает модель
		б) обрабатывает полученные данные
		в) подключает вид и отдает туда данные